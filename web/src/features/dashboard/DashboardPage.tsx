import { Title, Text, Stack } from '@mantine/core'

// Placeholder from wave 0. Owned by WP-11 (docs/WORKPLAN.md).
export function DashboardPage() {
  return (
    <Stack>
      <Title order={2}>Dashboard</Title>
      <Text c="dimmed">Not implemented yet (WP-11).</Text>
    </Stack>
  )
}
