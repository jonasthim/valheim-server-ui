import { Title, Text, Stack } from '@mantine/core'

// Placeholder from wave 0. Owned by WP-10 (docs/WORKPLAN.md).
export function AccountPage() {
  return (
    <Stack>
      <Title order={2}>Account</Title>
      <Text c="dimmed">Not implemented yet (WP-10).</Text>
    </Stack>
  )
}
