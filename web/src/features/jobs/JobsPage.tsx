import { Title, Text, Stack } from '@mantine/core'

// Placeholder from wave 0. Owned by WP-14 (docs/WORKPLAN.md).
export function JobsPage() {
  return (
    <Stack>
      <Title order={2}>Jobs</Title>
      <Text c="dimmed">Not implemented yet (WP-14).</Text>
    </Stack>
  )
}
