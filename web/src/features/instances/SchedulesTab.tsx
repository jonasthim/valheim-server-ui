import { Text } from '@mantine/core'

// Placeholder from wave 0. Owned by WP-12 (docs/WORKPLAN.md). Props: the instance id.
export function SchedulesTab({ id }: { id: string }) {
  return <Text c="dimmed">Schedules tab for {id} not implemented yet (WP-12).</Text>
}
