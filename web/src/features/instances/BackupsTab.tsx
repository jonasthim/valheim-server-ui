import { Text } from '@mantine/core'

// Placeholder from wave 0. Owned by WP-12 (docs/WORKPLAN.md). Props: the instance id.
export function BackupsTab({ id }: { id: string }) {
  return <Text c="dimmed">Backups tab for {id} not implemented yet (WP-12).</Text>
}
