import { Text } from '@mantine/core'

// Placeholder from wave 0. Owned by WP-11 (docs/WORKPLAN.md). Props: the instance id.
export function ConsoleTab({ id }: { id: string }) {
  return <Text c="dimmed">Console tab for {id} not implemented yet (WP-11).</Text>
}
