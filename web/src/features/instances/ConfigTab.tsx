import { Text } from '@mantine/core'

// Placeholder from wave 0. Owned by WP-11 (docs/WORKPLAN.md). Props: the instance id.
export function ConfigTab({ id }: { id: string }) {
  return <Text c="dimmed">Config tab for {id} not implemented yet (WP-11).</Text>
}
