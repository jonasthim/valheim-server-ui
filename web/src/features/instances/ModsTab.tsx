import { Text } from '@mantine/core'

// Placeholder from wave 0. Owned by WP-13 (docs/WORKPLAN.md). Props: the instance id.
export function ModsTab({ id }: { id: string }) {
  return <Text c="dimmed">Mods tab for {id} not implemented yet (WP-13).</Text>
}
