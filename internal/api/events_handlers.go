package api

import "github.com/go-chi/chi/v5"

// registerEventRoutes mounts the events endpoints. Stub from wave 0; see
// docs/WORKPLAN.md for the owning work package and docs/openapi.yaml for the
// contract. Replace the body entirely; keep the function name.
func registerEventRoutes(r chi.Router, d *Deps) {
	_ = d
	_ = r
}
